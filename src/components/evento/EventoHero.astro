---
import type { EventoData } from '../../lib/eventos';

interface Props {
  evento: EventoData;
}

const { evento } = Astro.props;

const lugarCompleto = [evento.lugar.nombre, evento.lugar.detalle].filter(Boolean).join(', ');
const tieneFondo = Boolean(evento.imagenes.hero && evento.imagenes.hero.trim());
---

<header class={`evento-hero textura-anillos ${tieneFondo ? 'con-fondo' : ''}`} id="hero">
  {tieneFondo && (
    <div class="hero-fondo" style={`background-image: url(${evento.imagenes.hero})`}>
      <div class="hero-velo" aria-hidden="true"></div>
    </div>
  )}

  <div class="hero-contenido">
    <img
      src="/assets/logos/marypepa-logo.png"
      alt="Vinos con Marypepa"
      width="84"
      height="84"
      class="hero-logo"
    />

    <div class="hero-eyebrow">
      {evento.presenta && <span class="presenta">{evento.presenta} presenta</span>}
      {evento.serie && evento.presenta && <span class="separador" aria-hidden="true">·</span>}
      {evento.serie && <span class="serie">{evento.serie}</span>}
    </div>

    <h1 class="hero-titulo">{evento.titulo}</h1>

    {evento.bajada && <p class="hero-bajada">{evento.bajada}</p>}

    <div class="hero-datos-clave">
      {evento.fecha.textoVisible && (
        <span class="dato-item">{evento.fecha.textoVisible}</span>
      )}

      {lugarCompleto && (
        <>
          <span class="dato-separador" aria-hidden="true">&middot;</span>
          <span class="dato-item">{lugarCompleto}</span>
        </>
      )}

      {evento.precio.textoVisible && (
        <>
          <span class="dato-separador" aria-hidden="true">&middot;</span>
          <span class="dato-item dato-precio">{evento.precio.textoVisible}</span>
        </>
      )}
    </div>

    <div class="hero-scroll-hint">
      <a href="#entradas" class="hero-link-entradas">
        Ver detalles y entradas &darr;
      </a>
    </div>
  </div>
</header>

<style>
  .evento-hero {
    position: relative;
    padding: 2.5rem 1.5rem 2.5rem;
    text-align: center;
    border-bottom: 1px solid var(--border);
  }

  .evento-hero.con-fondo {
    padding: 3.5rem 1.5rem 3rem;
    border-bottom: none;
    color: var(--crema);
    overflow: hidden;
    background: var(--accent-oscuro);
  }

  .hero-fondo {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center 65%;
    z-index: 0;
  }

  .hero-velo {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(15, 10, 9, 0.8) 0%,
      rgba(30, 6, 8, 0.72) 45%,
      rgba(10, 6, 6, 0.9) 100%
    );
  }

  .hero-contenido {
    position: relative;
    z-index: 1;
    max-width: 640px;
    margin: 0 auto;
  }

  .hero-logo {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    display: block;
    margin: 0 auto 1.25rem;
    box-shadow: 0 0 0 4px rgba(242, 240, 228, 0.9), 0 4px 20px rgba(0, 0, 0, 0.35);
  }

  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.5rem;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--muted);
    margin-bottom: 0.75rem;
  }

  .con-fondo .hero-eyebrow {
    color: var(--crema);
    opacity: 0.85;
  }

  .hero-eyebrow .separador {
    color: var(--border);
  }

  .con-fondo .hero-eyebrow .separador {
    color: var(--crema);
    opacity: 0.4;
  }

  .hero-titulo {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 600;
    font-size: clamp(2.5rem, 9vw, 4.5rem);
    line-height: 1.05;
    color: var(--accent);
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
  }

  .con-fondo .hero-titulo {
    color: var(--crema);
    text-shadow: 0 2px 28px rgba(0, 0, 0, 0.4);
  }

  .hero-bajada {
    max-width: 480px;
    margin: 0 auto 1.75rem;
    font-size: 1.0625rem;
    line-height: 1.55;
    color: var(--muted);
  }

  .con-fondo .hero-bajada {
    color: var(--crema);
    opacity: 0.94;
  }

  .hero-datos-clave {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.5rem 0.6rem;
    margin: 0 auto 1.75rem;
    font-size: 0.9375rem;
    font-weight: 500;
    line-height: 1.4;
  }

  .dato-item {
    color: var(--text);
  }

  .con-fondo .dato-item {
    color: var(--crema);
  }

  .dato-separador {
    color: var(--border);
  }

  .con-fondo .dato-separador {
    color: var(--crema);
    opacity: 0.4;
  }

  .dato-precio {
    font-weight: 700;
    color: var(--accent);
  }

  .con-fondo .dato-precio {
    color: var(--crema);
  }

  .hero-scroll-hint {
    margin-top: 0.25rem;
  }

  .hero-link-entradas {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--muted);
    text-decoration: none;
    padding: 0.35rem 0.75rem;
    border-radius: 999px;
    transition: color 0.15s ease, background-color 0.15s ease;
  }

  .con-fondo .hero-link-entradas {
    color: var(--crema);
    opacity: 0.85;
  }

  .hero-link-entradas:hover {
    color: var(--accent);
    background-color: var(--accent-lt);
  }

  .con-fondo .hero-link-entradas:hover {
    color: var(--crema);
    opacity: 1;
    background-color: rgba(250, 250, 247, 0.12);
  }

  @media (min-width: 640px) {
    .evento-hero {
      padding: 4rem 1.5rem 3.5rem;
    }

    .evento-hero.con-fondo {
      padding: 6rem 1.5rem 4.5rem;
    }

    .hero-datos-clave {
      font-size: 1.0625rem;
    }

    .hero-logo {
      width: 80px;
      height: 80px;
      margin-bottom: 1.5rem;
    }
  }
</style>
